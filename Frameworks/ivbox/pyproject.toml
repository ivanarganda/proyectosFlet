[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ivbox"
version = "0.3.1"
description = "Improved call import"
readme = "README.md"
authors = [
    { name = "Ivan Gonzalez", email = "ivanartista96@example.com" }
]
requires-python = ">=3.10"

dependencies = [
    "polars",
    "pandas",
    "numpy",
    "requests",
    "flet"
]

[tool.setuptools.packages.find]
where = ["."]